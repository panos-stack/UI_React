
  # Interactive Gradient Background

  This is a code bundle for Interactive Gradient Background. The original project is available at https://www.figma.com/design/hVVExosM78ZkSCToumeX8m/Interactive-Gradient-Background.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  